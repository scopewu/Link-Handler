# 更新日志

## Version 1.7.0

### 无障碍

- **ARIA 标签页与键盘导航**
  设置页标签页遵循 WAI-ARIA Tabs 模式：`role=tablist/tab/tabpanel`、`aria-selected`、
  动态 tabindex，支持方向键/Home/End 导航并自动激活。

- **语义化 HTML**
  选项页与 popup 改用地标元素（`main`/`header`/`footer`/`section`）、搜索框使用
  `search` 元素，白名单改用真正的 `ul` 列表。白名单添加改为表单提交；标签与
  输入框建立关联。

- **弹窗键盘支持**
  规则弹窗将 Tab 焦点限制在弹窗内，支持 Esc 关闭，关闭后焦点归还触发元素；
  打开时自动聚焦首个输入框。

- **可操作控件与播报**
  标签删除控件改为真正的按钮并具备无障碍名称；装饰性 SVG 标记 `aria-hidden`；
  纯图标按钮提供 `aria-label`；提示消息通过 `aria-live` 播报（错误为
  `assertive`）。

- **焦点可见与对比度**
  键盘焦点通过 `:focus-visible` 样式可见（不影响鼠标点击）。辅助文字颜色满足
  白底 WCAG AA 对比度。`[hidden]` 以 `!important` 兜底，类选择器的 `display`
  声明不会让隐藏元素意外可见。

### 问题修复

- **`escapeHtml` 引号转义**
  `escapeHtml` 的输出会用于双引号属性值（`aria-label`），但此前只转义 `& < >`。
  现已转义引号，堵住经由导入配置值进行属性注入的路径。

- **i18n 占位符缺失**
  新增的 `removeTag` 消息使用了 `$1$` 却未定义 `placeholders`，导致 Chrome 报
  `Variable $1$ used but not defined` 且 `getMessage` 返回空字符串。现已在全部
  语言定义 `placeholders`，同时 i18n 回退实现对齐 `chrome.i18n`（命名占位符
  展开、字符串参数、安全替换）。

### 其他变更

- 移除内置百度跟踪规则（优先使用按域名覆盖）。
- 仅保留错误级控制台输出，移除信息级日志。
- README 与当前规则和功能同步。

---

## Version 1.6.1

### 问题修复

- **地址栏清洗改为配置驱动**
  MAIN world 的 SPA 钩子不再硬编码 Bilibili 跟踪参数列表。`content.js` 现在通过
  `sanitize-config` 消息下发清洗映射，取自**所有启用且配置了 `cleanUrlParams` 的跟踪规则**
  ——地址栏清洗是跟踪清理的默认行为，与链接清洗共用同一参数列表。在选项页修改规则会
  立即反映到地址栏；禁用该规则、将站点加入白名单或关闭跟踪清理总开关后，地址栏清洗
  也随之关闭。

- **简书重定向规则**
  对照简书站内真实链接核实：跳转格式为 `links.jianshu.com/go?to=...`，解包参数为 `to`
  （路径 `/go`，域名经后缀匹配覆盖 `links.jianshu.com` 子域）。README 示例已同步。

- **移除无效的 t.cn 规则**
  t.cn 短链不携带 `url` 查询参数，该规则永远无法匹配，已从内置重定向规则中移除。

- **重扫合并**
  popup 的手动处理消息与 `chrome.storage.onChanged` 在每次保存配置时几乎同时触发，
  导致整页重复重扫。两条路径现共用一次防抖重扫。

- **最小权限**
  移除 `host_permissions: <all_urls>`（manifest 声明的 content script 不需要它）；
  新增 `activeTab` 权限，popup 仍可读取当前标签页 URL。

- **`mergeConfig` 更名为 `applyStoredConfig`**
  旧名称暗示只处理 v1 遗留数据；实际按 `stored.version` 分发（v2 diff →
  `applyConfigDiff`，其他 → 默认配置）。

### UI 增强

- **「全局跟踪参数」卡片迁移**
  全局跟踪参数编辑器从「通用」标签页移至「跟踪清理」标签页，与其叠加生效的按域名
  跟踪规则归为一组。

### 测试

- 新增 `tests/config.test.mjs`（内置 `node:test`，零依赖），覆盖 diff 往返、
  规范化规则比较、回落行为与 `buildSanitizeHostMap()`。运行：`node --test`。

---

## Version 1.6.0

### 新功能

- **全局跟踪参数清理**
  新增全局参数黑名单，对所有网站的链接统一清除通用跟踪参数（`utm_source`、`utm_medium`、`utm_campaign`、`utm_term`、`utm_content`、`fbclid`、`gclid`、`msclkid`、`mc_cid`、`mc_eid`）。与按域名跟踪规则叠加生效，跟随跟踪清理总开关。可在「通用」标签页编辑。

### 问题修复

- **统计计数**
  按域名规则与全局清理不再对同一条链接重复计入「已清理」统计。

- **URL 清理一致性**
  通配符（`*`）清理参数时现在会记录改写后的 `href`，修复了后续变更可能被误判的潜在不一致。

---

## Version 1.5.2

### 新功能

- **选项页使用说明**
  在选项页面新增「使用说明」标签页，文档化介绍每项配置的含义，包括重定向规则、跟踪清理规则、白名单机制和备份恢复。

---

## Version 1.5.1

### 新功能

- **拦截 Bilibili 地址栏跟踪参数**
  MAIN 世界 SPA 钩子现在会拦截 `bilibili.com` 上的 `history.pushState`/`replaceState`，在写入浏览器历史之前移除跟踪参数（包括 `vd_source`）。页面首次加载时也会清洗当前地址栏。

---

## Version 1.5.0

### 新功能

- **MAIN 世界 SPA 导航钩子**
  新增 `spa-hook.js`，注入页面的 `MAIN` 世界拦截 `history.pushState`/`replaceState` 和 `popstate`，并通过 `postMessage` 通知内容脚本。修复了隔离世界历史方法补丁无法捕获真实 SPA 导航的问题。

- **配置实时更新**
  内容脚本监听 `chrome.storage.onChanged`，配置变更后实时生效，无需刷新页面。

- **规则路径作用域**
  重定向规则新增可选 `pathPattern`，同一域名可针对不同的跳转端点设置不同规则（如 Facebook `/flx/warn/...`、少数派 `/go/...`、简书 `/p/...`）。

### 改进

- **差异式配置存储（v2）**
  用户设置以与默认配置的差异形式存储（自定义规则、内置规则覆盖、已删除的内置规则）。升级后内置规则更新可以同步到现有用户，而不是固定在安装时的版本。

- **更安全的 Bilibili 参数清理**
  将 Bilibili 跟踪参数规则中的 `*` 通配符替换为明确黑名单，保留 `t`（时间点）、`p`（分 P）等功能性参数。

- **更可靠的点击委托**
  委托的点击重写守卫现在在 document 捕获阶段执行，可靠拦截中键和带修饰键的点击。

- **模态框表单加固**
  编辑规则时保留未知字段；保存时收集未提交的标签输入；模态框表单增加属性注入防护。

- **优化 MutationObserver 重复处理**
  将规范化后的 `href` 存入 `data-` 属性，避免对有效 URL 未变化的链接重复处理。

### 清理

- 移除未使用的 `processExistingLinks` 设置及残留的轮询代码。

---

## Version 1.4.2

### 问题修复

- **修复 SPA 链接处理失败**
  修复了由于 `requestIdleCallback` 参数错误导致 SPA 导航时链接处理失败的问题。

### 改进

- **强化链接处理与 XSS 防护**
  加强 URL 验证与清理，防止重定向解包中的潜在 XSS 攻击向量。

### 文档

- **清理 README**
  移除表情符号和装饰元素，更加专业；新增中文 README (`README.zh_CN.md`)。

---

## Version 1.4.0

### 新功能

- **域名白名单**
  新增按域名白名单跳过链接处理功能。支持后缀匹配（如 `deepseek.com` 自动覆盖 `chat.deepseek.com`）。可在选项页面管理，也可直接在弹出窗口切换当前网站。

- **白名单继承提示**
  当子域名通过父域名继承白名单状态时，弹出窗口开关自动置灰并显示来源域名，避免无法关闭的困惑。

### 改进

- **移除全局开关**
  移除了粗放的全局启用/禁用开关，改为按网站细粒度控制。

### 问题修复

- **修复批处理死锁**
  修复 `batchProcessLinks` 传入空数组时直接返回、导致剩余链接永远不被处理的关键 Bug（仅前 100 条生效）。

- **修复重新处理逻辑**
  `reprocess` 现在会先清除已处理标记再全量重新扫描，而不是只碰未处理的链接。

- **修复 SPA 历史方法重复包装**
  增加防护，防止扩展重载时对 `history.pushState/replaceState` 重复包装。

### 清理

- 移除失效的 SegmentFault (`link.segmentfault.com`) 重定向规则。

---

## Version 1.3.0

### 新功能

- **链接处理统计**
  在弹出窗口中实时显示已处理的链接数量。统计包括：总处理数、重定向解包数、target 移除数和跟踪清理数。

- **模态框添加规则**
  新增直观的模态对话框用于创建重定向和跟踪规则，支持内置验证。

- **搜索与过滤**
  在选项页面添加搜索功能，快速查找特定规则。

- **配置自动保存**
  设置更改后自动保存，无需手动点击保存按钮。

### 改进

- **增强的 URL 验证**
  改进 URL 解析和验证逻辑，提升安全性和兼容性。

- **改进的同源处理**
  优化同域名链接的 `target="_blank"` 移除逻辑。

- **URL 参数清理**
  增强跟踪参数移除功能，支持通配符（`*` 移除所有参数）。

- **标签输入体验**
  改进规则编辑模态框中的标签输入处理，优化键盘导航体验。

### 清理

- 移除未使用的 `purify.min.js` 依赖。

---

## Version 1.2.0

### 新功能

- **Firefox 支持**
  添加 Firefox 浏览器兼容性支持（Manifest V3）。

- **SPA 导航支持**
  单页应用路由变化时自动重新处理链接。

### UI 增强

- 重新设计选项页面，采用标签页界面。

- 更新扩展图标，提升可见性。

---

**完整更新日志**: 在 GitHub 上查看与之前版本的对比。
